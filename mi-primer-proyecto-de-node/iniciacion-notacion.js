// Notacion



// ; -> Delimitar el final de una linea de codigo
const b = 4;

b + 4;

// . -> se utiliza en los objetos para acceder a los atributos.
// movil.ancho;


// []  -> Se utilizan para las listas , arreglos o arrays
const lista = [1,2,3,4];
console.log(lista[2]);

// () -> Se utilizan para las funciones

function suma (a,b){
    //se hace la suma
}

// {} -> Llaves para objetos, funciones y estructuras de control.

const movil = {
    anchura:5,
    altura:10,
}

if(movil.altura == 10)
{
    console.log("Nuestro celular tienee un ancho de 10 mm");
}

